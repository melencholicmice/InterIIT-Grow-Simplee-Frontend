import React from 'react';
import { useRoutes } from 'react-router-dom';
import PackageDataDimentions from "../PackageDataDimentions"
import PackageDataLayout from '../PackageDataLayout';
import PackageDataLocation from '../PackageDataLocation';
import PackageDataStatus from '../PackageDataStatus';

const PackageRoutes = () => {
  return (
    <>
      {/* PackageRoutes */}
    </>
  )
}

export default PackageRoutes;